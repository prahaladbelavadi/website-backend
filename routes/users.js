var express = require('express');
var router = express.Router();

// handle authentication here
//  hand routing to another file that handles mongodb stuff

router.get('/', function (req, res, next) {
    // 
});

module.exports = router;
